# 09-3. LSTM과 GRU 셀 스터디 상세 요약

## * 개념과 활용법 (동영상 스크립트 요약)

요약하자면, 이 강의는 순환 신경망의 한 종류인 **LSTM**과 **GRU** 셀의 개념과 활용법을 설명합니다.

---

### LSTM 셀
**LSTM(Long Short-Term Memory)**은 긴 문맥의 정보를 기억하지 못하는 기존 순환 신경망의 문제를 개선한 것입니다.

* **구조:** 기존의 '은닉 상태' 외에 '셀 상태'라는 새로운 개념이 추가되어 더 많은 정보를 저장하고 전달할 수 있습니다.
* **복잡한 연산:** 삭제 게이트, 입력 게이트, 출력 게이트 등 다양한 게이트와 여러 개의 가중치를 사용해 복잡한 계산을 합니다.
* **사용법:** 복잡한 내부 구조와 달리, 케라스(Keras) 라이브러리의 `LSTM` 클래스를 사용하면 간단하게 모델을 만들 수 있습니다.

---

### 여러 개의 순환층 연결하기
모델의 성능을 높이기 위해 순환층을 여러 개 쌓을 수 있습니다. 이때, 마지막 층을 제외한 모든 순환층에는 `return_sequences=True` 옵션을 설정하여 모든 타임스텝의 출력을 다음 층으로 전달해야 합니다.

---

### GRU 셀
**GRU(Gated Recurrent Unit)**는 LSTM의 간소화된 버전입니다.

* **구조:** LSTM보다 간단한 구조를 가지고 있으며, LSTM의 셀 상태와 은닉 상태를 하나로 합쳐서 사용합니다.
* **사용법:** LSTM과 마찬가지로 케라스의 `GRU` 클래스를 사용하면 쉽게 모델을 구성할 수 있습니다.

---

### 핵심 요약
LSTM과 GRU는 기존 순환 신경망보다 복잡한 구조를 가졌지만, 이를 통해 더 많은 정보를 기억하고 복잡한 패턴을 학습할 수 있습니다. 프로그래밍 시에는 파이썬의 `keras` 라이브러리를 사용해 `LSTM`이나 `GRU` 클래스로 간단하게 구현할 수 있습니다.

---

## * 소스 설명 (제미나이 cli)

## 1. 개요: 순환 신경망의 발전

이전 스터디에서 다룬 순환 신경망(RNN)은 시퀀스 데이터 처리에 유용하지만, 시퀀스가 길어질수록 앞쪽의 정보가 뒤로 충분히 전달되지 못하는 **장기 의존성 문제(Long-Term Dependency Problem)**가 있었습니다. 이를 해결하기 위해 제안된 것이 **LSTM(Long Short-Term Memory)**과 **GRU(Gated Recurrent Unit)** 셀입니다.

이 노트북에서는 IMDB 영화 리뷰 데이터셋을 사용하여, LSTM과 GRU를 활용한 감성 분류 모델을 구축하고 그 원리와 성능을 심층적으로 분석합니다.

## 2. 데이터 준비 및 전처리

감성 분류 모델을 훈련하기 위해 텍스트 데이터를 숫자 시퀀스로 변환하는 과정입니다.

- **데이터셋 로드**: `keras.datasets.imdb`를 사용합니다. `num_words=500` 파라미터는 훈련에 사용할 단어의 수를 가장 자주 등장하는 500개로 제한한다는 의미입니다. 리뷰 텍스트는 각 단어가 정수 인덱스로 변환된 형태로 제공됩니다.

- **훈련/검증 세트 분리**: 로드한 훈련 데이터를 다시 훈련 세트(80%)와 검증 세트(20%)로 나누어 모델 훈련 시 과대적합 여부를 모니터링합니다.

- **패딩(Padding)**: 각 리뷰(시퀀스)의 길이가 다르기 때문에, 모델에 입력하기 위해 길이를 통일해야 합니다. `pad_sequences`를 사용하여 모든 시퀀스의 길이를 100으로 맞춥니다.
    - `maxlen=100`: 시퀀스의 최대 길이를 100으로 지정합니다.
    - 길이가 100보다 길면 앞부분을 잘라내고, 100보다 짧으면 앞부분을 0으로 채웁니다(pre-padding).

```python
from keras.datasets import imdb
from sklearn.model_selection import train_test_split
from keras.preprocessing.sequence import pad_sequences

# IMDB 데이터셋 로드 (상위 500개 단어만 사용)
(train_input, train_target), (test_input, test_target) = imdb.load_data(num_words=500)

# 훈련 세트를 훈련/검증 세트로 분리
train_input, val_input, train_target, val_target = train_test_split(
    train_input, train_target, test_size=0.2, random_state=42)

# 시퀀스 길이를 100으로 패딩
train_seq = pad_sequences(train_input, maxlen=100)
val_seq = pad_sequences(val_input, maxlen=100)
```

## 3. LSTM (Long Short-Term Memory) 신경망 모델

LSTM은 내부에 **셀 상태(Cell State)**라는 정보 흐름 경로를 추가하고, **게이트(Gate)**를 이용해 이 상태를 제어함으로써 장기 기억을 효과적으로 처리합니다.

### 3.1. 기본 LSTM 모델 구축

- **`Embedding` 층**: 단어(정수 인덱스)를 밀집 벡터로 변환합니다. `Embedding(500, 16)`는 500개의 단어를 16차원 벡터로 매핑합니다.
- **`LSTM` 층**: 순환 구조의 핵심으로, 8개의 뉴런(유닛)을 가집니다. 이 숫자는 출력 공간의 차원을 의미합니다.
- **`Dense` 층**: 최종 출력을 생성합니다. 긍정(1)과 부정(0)을 분류하는 이진 분류 문제이므로, 1개의 뉴런과 `sigmoid` 활성화 함수를 사용합니다.

```python
import keras

model_lstm = keras.Sequential()
model_lstm.add(keras.layers.Embedding(500, 16, input_length=100))
model_lstm.add(keras.layers.LSTM(8))
model_lstm.add(keras.layers.Dense(1, activation='sigmoid'))

model_lstm.summary()
```

- **모델 컴파일 및 훈련**:
    - `optimizer='adam'`: 효율적인 경사 하강법 알고리즘인 Adam을 사용합니다.
    - `loss='binary_crossentropy'`: 이진 분류 문제에 적합한 손실 함수입니다.
    - `callbacks`: 훈련 중 특정 시점에 원하는 동작을 수행하도록 합니다.
        - `ModelCheckpoint`: 검증 손실이 가장 낮은 최적의 모델 가중치를 파일로 저장합니다.
        - `EarlyStopping`: 검증 손실이 일정 횟수(patience=3) 이상 개선되지 않으면 훈련을 조기 종료하여 과대적합을 방지합니다.

### 3.2. 과대적합 제어를 위한 드롭아웃

훈련 결과, 훈련 손실은 계속 감소하지만 검증 손실은 특정 지점부터 다시 상승하는 과대적합 현상이 나타났습니다. 이를 해결하기 위해 드롭아웃을 적용합니다.

- **`dropout=0.2`**: LSTM 층에 드롭아웃을 적용합니다. 이는 순환 층의 입력과 출력에 대해 20%의 특성을 랜덤하게 0으로 만들어, 모델이 특정 패턴에 과도하게 의존하는 것을 방지합니다.

```python
# 드롭아웃을 적용한 LSTM 모델
model_dropout = keras.Sequential()
model_dropout.add(keras.layers.Embedding(500, 16, input_length=100))
model_dropout.add(keras.layers.LSTM(8, dropout=0.2))
model_dropout.add(keras.layers.Dense(1, activation='sigmoid'))
```
드롭아웃 적용 후, 훈련 손실과 검증 손실의 차이가 줄어들어 과대적합이 완화된 것을 확인할 수 있습니다.

### 3.3. 2개 층 연결 (모델 깊이 증가)

모델의 표현력을 높이기 위해 순환 층을 여러 개 쌓을 수 있습니다. 이때 주의할 점은, 순환 층을 다음 순환 층으로 연결하려면 모든 타임스텝의 출력을 전달해야 한다는 것입니다.

- **`return_sequences=True`**: 앞선 LSTM 층의 파라미터로 설정하여, 마지막 타임스텝의 은닉 상태만 출력하는 것이 아니라 모든 타임스텝의 은닉 상태를 시퀀스 형태로 출력하여 다음 LSTM 층의 입력으로 사용합니다.

```python
# 2개의 LSTM 층을 쌓은 모델
model_2lstm = keras.Sequential()
model_2lstm.add(keras.layers.Embedding(500, 16, input_length=100))
model_2lstm.add(keras.layers.LSTM(8, dropout=0.2, return_sequences=True))
model_2lstm.add(keras.layers.LSTM(8, dropout=0.2))
model_2lstm.add(keras.layers.Dense(1, activation='sigmoid'))
```

## 4. GRU (Gated Recurrent Unit) 신경망 모델

GRU는 LSTM의 복잡한 구조를 간소화한 셀입니다. **업데이트 게이트**와 **리셋 게이트** 두 개의 게이트만으로 LSTM과 유사한 성능을 내며, 파라미터 수가 적어 계산 비용이 낮다는 장점이 있습니다.

- **모델 구조**: 이전 LSTM 모델과 동일한 구조에서 `LSTM` 층만 `GRU` 층으로 교체합니다.

```python
# GRU 모델 정의
model_gru = keras.Sequential()
model_gru.add(keras.layers.Embedding(500, 16, input_length=100))
model_gru.add(keras.layers.GRU(8, dropout=0.2))
model_gru.add(keras.layers.Dense(1, activation='sigmoid'))

model_gru.summary()
```
GRU 모델은 LSTM 모델보다 파라미터 수가 적지만, 훈련 결과 검증 손실이 더 낮게 나타나 이 문제에서는 더 효율적이었습니다.

## 5. 최종 평가 및 결론

- **최종 모델 평가**: 훈련된 모델 중 가장 성능이 좋았던 GRU 모델(`best-gru-model.keras`)을 로드하여, 따로 분리해 두었던 테스트 세트로 최종 성능을 평가합니다.

```python
# 테스트 세트로 최종 모델 평가
test_seq = pad_sequences(test_input, maxlen=100)
best_model = keras.models.load_model('best-gru-model.keras')
best_model.evaluate(test_seq, test_target)
# 최종 결과: [loss: 0.4206, accuracy: 0.8072]
```

- **결론**:
    - LSTM과 GRU는 RNN의 장기 의존성 문제를 해결하는 데 효과적인 셀 구조입니다.
    - 드롭아웃은 순환 층의 과대적합을 방지하는 데 중요한 역할을 합니다.
    - GRU는 LSTM보다 구조가 간단하고 파라미터가 적어 훈련 속도가 빠르면서도, 유사하거나 더 나은 성능을 보일 수 있어 효율적인 선택지가 될 수 있습니다.
    - 최종적으로 GRU 모델을 사용하여 IMDB 감성 분류 문제에서 약 80.7%의 테스트 정확도를 달성했습니다.
