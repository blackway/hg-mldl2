# `09-2.ipynb` 소스 분석 보고서

이 문서는 `09-2.ipynb` Jupyter Notebook 소스 코드의 구조, 목적, 그리고 핵심 로직을 상세히 분석합니다.

## 1. 노트북의 핵심 목표

이 노트북의 주요 목표는 **IMDB 영화 리뷰 데이터셋을 사용하여 순환 신경망(RNN) 모델로 긍정/부정 리뷰를 분류(감성 분석)**하는 것입니다.

전체적인 흐름은 텍스트 데이터를 처리하는 두 가지 주요 접근 방식을 비교하며 진행됩니다.
1.  **SimpleRNN + 원-핫 인코딩 (One-Hot Encoding)**
2.  **SimpleRNN + 단어 임베딩 (Word Embedding)**

---

## 2. 단계별 상세 분석

### 2.1. 데이터 준비 및 전처리

모든 머신러닝 프로젝트와 마찬가지로, 모델에 주입할 데이터를 준비하는 과정부터 시작합니다.

-   **데이터 로드**: `keras.datasets.imdb`에서 IMDB 리뷰 데이터셋을 불러옵니다. 이때 `num_words` 파라미터는 훈련 데이터에서 가장 자주 나타나는 단어의 개수를 제한하는 역할을 합니다. 각 단어는 고유한 정수로 이미 변환되어 있습니다.

-   **데이터 탐색**:
    -   로드된 데이터의 형태(훈련셋 25,000개, 테스트셋 25,000개)를 확인합니다.
    -   각 리뷰는 단어의 나열이므로 길이가 제각각 다릅니다. 코드에서는 각 리뷰의 평균 및 중간 길이를 계산하고, 히스토그램을 통해 길이 분포를 시각화하여 이후에 진행할 '패딩' 길이를 결정하는 근거로 삼습니다.

-   **훈련/검증 데이터 분리**: 훈련 데이터를 다시 훈련셋(80%)과 검증셋(20%)으로 나누어, 모델을 훈련하는 동안 성능을 객관적으로 평가하고 과대적합을 모니터링합니다.

-   **패딩 (Padding)**:
    -   RNN 모델에 데이터를 입력하려면 모든 샘플(리뷰)의 길이를 동일하게 맞춰야 합니다.
    -   `pad_sequences` 함수를 사용하여 모든 리뷰의 길이를 `maxlen=100`으로 통일합니다.
    -   리뷰 길이가 100보다 길면 뒷부분을 잘라내고, 100보다 짧으면 앞부분을 0으로 채웁니다(이를 'pre-padding'이라 합니다).

### 2.2. 첫 번째 모델: SimpleRNN과 원-핫 인코딩

이 방식은 단어를 벡터로 표현하는 가장 기본적인 방법 중 하나를 사용합니다.

-   **원-핫 인코딩**:
    -   패딩된 정수 시퀀스를 `keras.utils.to_categorical`을 사용해 원-핫 벡터로 변환합니다.
    -   예를 들어, 단어 사전의 크기가 200개라면 각 단어는 199개의 0과 1개의 1로 이루어진 200차원의 벡터로 표현됩니다.
    -   이 방식은 **메모리 사용량이 매우 크다**는 치명적인 단점이 있으며, 노트북 후반부에서 실제 메모리 점유량을 출력하며 이 문제를 명확히 보여줍니다.

-   **모델 구성**:
    -   `keras.Sequential` 모델을 생성합니다.
    -   `SimpleRNN` 레이어를 추가하여 시퀀스 데이터의 시간적 패턴을 학습하도록 합니다.
    -   마지막으로 `Dense` 레이어와 `sigmoid` 활성화 함수를 사용하여 긍정(1) 또는 부정(0)을 예측하는 이진 분류 모델을 완성합니다.

-   **모델 훈련 및 최적화**:
    -   `adam` 옵티마이저와 `binary_crossentropy` 손실 함수로 모델을 컴파일합니다.
    -   **콜백(Callbacks)**을 사용하여 훈련 과정을 최적화합니다.
        -   `ModelCheckpoint`: 검증 세트 기준 가장 성능이 좋은 모델의 가중치를 파일로 저장합니다.
        -   `EarlyStopping`: 검증 성능이 일정 기간(예: 3 에포크) 개선되지 않으면 훈련을 조기 종료하여 불필요한 훈련을 막고 과대적합을 방지합니다.
    -   `fit` 메소드로 모델을 훈련시키고, 훈련 과정의 손실(loss)과 정확도(accuracy)를 기록합니다.

### 2.3. 두 번째 모델: SimpleRNN과 단어 임베딩

이 방식은 원-핫 인코딩의 비효율성을 개선한, 현대 자연어 처리에서 표준적으로 사용되는 강력한 방법입니다.

-   **단어 임베딩 (Word Embedding)**:
    -   원-핫 인코딩 대신, 정수 시퀀스를 그대로 모델에 입력합니다.
    -   모델의 첫 번째 레이어로 `Embedding` 레이어를 추가합니다.
    -   **`Embedding` 레이어의 역할**:
        1.  각 정수(단어)를 지정된 차원(코드에서는 16)의 저차원 밀집 벡터(dense vector)로 변환합니다.
        2.  이 변환 규칙은 훈련 과정에서 모델의 다른 가중치와 함께 **스스로 학습**됩니다. 즉, 단어 간의 의미 있는 관계(유사성 등)를 스스로 학습하여 벡터 공간에 표현하게 됩니다.
    -   이 방식은 원-핫 인코딩보다 훨씬 **메모리 효율적**이며, 단어의 의미를 벡터에 함축하므로 **일반적으로 성능도 더 우수**합니다.

-   **모델 구성 및 훈련**:
    -   `Embedding` 레이어를 제외한 나머지 구조(`SimpleRNN`, `Dense`)는 첫 번째 모델과 동일합니다.
    -   동일한 방식으로 모델을 컴파일하고 훈련하며, 입력 데이터로는 패딩된 정수 시퀀스를 직접 사용합니다.

---

## 3. 결론 및 핵심 요약

이 노트북은 RNN을 이용한 텍스트 분류의 기초를 다루는 체계적인 예제입니다.

-   **핵심**: 텍스트 데이터를 RNN 모델이 처리할 수 있는 숫자 형태의 텐서로 변환하는 전체 과정(정수 인코딩, 패딩)을 명확하게 보여줍니다.
-   **주요 비교점**: **원-핫 인코딩**과 **단어 임베딩**이라는 두 가지 핵심적인 단어 표현 방식을 코드로 직접 구현하고 결과를 비교하여 각 방법의 장단점을 명확하게 보여줍니다.
-   **교육적 가치**: 단어 임베딩이 메모리 효율성과 성능 면에서 왜 원-핫 인코딩보다 훨씬 우수한지를 실습을 통해 자연스럽게 체득하게 하며, 자연어 처리에서 단어 임베딩이 표준 기술로 자리 잡은 이유를 이해시켜 줍니다.
