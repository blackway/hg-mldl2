# 10-2.ipynb: 트랜스포머로 상품 설명 요약하기 (분석 자료)

## 1. 개요

이 노트북은 허깅페이스(Hugging Face)의 `transformers` 라이브러리를 사용하여 트랜스포머(Transformer) 기반의 모델로 텍스트 요약(Text Summarization)을 수행하는 방법을 다룹니다.

- **목표**: 긴 상품 설명이나 기사 내용을 짧고 핵심적인 문장으로 요약합니다.
- **주요 기술**: 트랜스포머 아키텍처, 특히 BART 모델을 사용합니다.
- **사용 라이브러리**: `transformers`
- **핵심 개념**:
    - 허깅페이스 `pipeline`을 이용한 간편한 모델 사용법
    - 영어 텍스트 요약과 한국어 텍스트 요약의 차이점
    - BART 모델의 구조와 설정 (Config)
    - 트랜스포머 모델의 토큰화(Tokenization) 과정

---

## 2. 코드 분석

### 2.1. 허깅페이스 `pipeline`으로 모델 로드하기

가장 먼저 `transformers` 라이브러리에서 `pipeline` 함수를 임포트합니다. 이 함수는 특정 NLP 작업을 매우 쉽게 수행할 수 있도록 도와주는 고수준 API입니다.

```python
from transformers import pipeline
```

#### 영어 요약 모델 (`distilbart-cnn-12-6`)

- **코드**:
  ```python
  pipe = pipeline(task='summarization',
                  model='sshleifer/distilbart-cnn-12-6', device=0)
  ```
- **분석**:
    - `task='summarization'`: `pipeline`에게 "요약" 작업을 수행할 것이라고 알려줍니다.
    - `model='sshleifer/distilbart-cnn-12-6'`: 사용할 모델을 지정합니다.
        - `distilbart`: BART 모델의 경량화 버전입니다. 더 적은 파라미터로 더 빠르게 동작하면서도 준수한 성능을 보입니다.
        - `cnn`: CNN/DailyMail 데이터셋으로 fine-tuning 되었음을 의미합니다. 이 데이터셋은 뉴스 기사와 요약문으로 구성되어 있어, 요약 작업에 특화되어 있습니다.
        - `12-6`: 인코더 레이어 12개, 디코더 레이어 6개를 의미합니다.
    - `device=0`: GPU를 사용하도록 설정합니다 (사용 가능한 경우).

- **실행 및 결과**:
  - **입력 (반 고흐 설명)**:
    ```
    Vincent Willem van Gogh was a Dutch Post-Impressionist painter who is among the most famous and influential figures in the history of Western art...
    ```
  - **출력 (요약문)**:
    ```
    Vincent Willem van Gogh was a Dutch Post-Impressionist painter . His oeuvre includes landscapes, still lifes, portraits and self-portraits . Van Gogh's work was beginning to gain critical attention before he died from a self-inflicted gunshot at age 37 .
    ```
  - **결과 분석**: 모델이 입력된 긴 텍스트의 핵심 내용(네덜란드 후기 인상파 화가, 작품 종류, 사망 원인 등)을 잘 추출하여 간결하게 요약했습니다.

#### 한국어 요약 모델 (`KoBART`)

- **코드**:
  ```python
  kobart = pipeline(task='summarization',
                    model='EbanLee/kobart-summary-v3', device=0)
  ```
- **분석**:
    - `model='EbanLee/kobart-summary-v3'`: 한국어 요약에 특화된 KoBART 모델을 사용합니다.
        - **KoBART**: SKT에서 개발한 한국어 BART 모델입니다. 한국어의 특성을 잘 이해하고 처리하도록 대규모 한국어 데이터로 사전 훈련되었습니다.
        - `EbanLee/kobart-summary-v3`: 이 모델은 한국어 문서 요약 데이터셋으로 추가 fine-tuning된 버전입니다.

- **실행 및 결과**:
  - **입력 (혼공 데이터 분석 책 소개)**:
    ```
    하나, ‘입문자 맞춤형 7단계 구성’을 따라가며 체계적으로 반복하는 탄탄한 학습 설계!
    이 책은 데이터 분석의 핵심 내용을 7단계에 걸쳐 반복 학습하면서 자연스럽게 머릿속에 기억되도록 구성했습니다...
    ```
  - **출력 (요약문)**:
    ```
    이 책은 데이터 분석의 핵심 내용을 7단계에 걸쳐 반복 학습하면서 머릿속에 기억되도록 구성했습니다. 독자 공부할 수 있는 커리큘럼을 그대로 믿고 끝까지 따라가다 보면 데이터 분석 공부가 난생 처음인 입문자도 무리 없이 책을 끝까지 마칠 수 있습니다...
    ```
  - **결과 분석**: 영어 모델과 마찬가지로, 한국어 모델도 긴 책 소개글의 핵심적인 특징(7단계 반복 학습, 입문자 맞춤, 저자 지원, 용어 노트 제공 등)을 잘 요약하여 보여줍니다.

---

### 2.2. 모델 내부 구조 살펴보기

`pipeline`으로 생성된 객체에서 `.model`과 `.tokenizer` 속성을 통해 모델의 세부 정보를 확인할 수 있습니다.

#### 모델 설정 (`kobart.model.config`)

- **코드**:
  ```python
  print(kobart.model.config)
  ```
- **주요 설정값 의미**:
    - `model_type`: "bart" (모델의 기본 아키텍처)
    - `vocab_size`: 30000 (모델이 이해하는 단어(토큰)의 개수)
    - `d_model`: 768 (트랜스포머의 인코더와 디코더에서 사용되는 기본 벡터 크기)
    - `encoder_layers`: 6 (인코더 레이어의 수)
    - `decoder_layers`: 6 (디코더 레이어의 수)
    - `encoder_attention_heads`: 16 (인코더의 어텐션 헤드 수)
    - `decoder_attention_heads`: 16 (디코더의 어텐션 헤드 수)
    - `is_encoder_decoder`: `True` (이 모델이 인코더-디코더 구조임을 명시)
    - `task_specific_params`:
        - `summarization`: 요약 작업에 대한 기본 파라미터 설정 (e.g., `max_length`, `min_length`, `num_beams`)

#### 모델 아키텍처 (`kobart.model`)

- **코드**:
  ```python
  kobart.model
  ```
- **구조 분석**:
    - `BartForConditionalGeneration`: 요약과 같은 조건부 생성 작업을 위한 BART 모델 클래스입니다.
    - `model (BartModel)`: 핵심적인 BART 모델 부분입니다.
        - `encoder (BartEncoder)`: 입력 텍스트를 이해하고 문맥 정보를 담은 벡터로 변환합니다. (6개의 `BartEncoderLayer`로 구성)
        - `decoder (BartDecoder)`: 인코더가 만든 문맥 벡터를 기반으로 요약문을 생성합니다. (6개의 `BartDecoderLayer`로 구성)
    - `lm_head`: 디코더의 최종 출력 벡터를 받아서 `vocab_size`(30000개)의 단어 중 다음 단어를 예측하는 역할을 하는 선형 레이어(Linear Layer)입니다.

---

### 2.3. 텍스트 토큰화 (Tokenization)

모델이 텍스트를 이해하려면, 먼저 숫자로 이루어진 토큰(Token)으로 변환해야 합니다. 이 과정을 **토큰화**라고 합니다.

- **어휘 사전 크기**:
  ```python
  print(kobart.tokenizer.vocab_size)
  # 출력: 30000
  ```
  KoBART 모델은 30,000개의 토큰으로 구성된 어휘 사전을 가집니다.

- **토큰화 과정**:
  1.  **`tokenize()`**: 문장을 토큰 단위로 분리합니다. 한국어의 경우, '음절'이나 '형태소'가 아닌, 통계적으로 자주 등장하는 글자 묶음(Subword)으로 나뉩니다. ` `(띄어쓰기 표시)는 단어의 시작을 의미합니다.
      ```python
      tokens = kobart.tokenizer.tokenize('혼자 만들면서 배우는 딥러닝')
      # 출력: [' 혼자', ' 만들', '면서', ' 배우는', ' ', '딥', '러', '닝']
      ```
  2.  **`convert_tokens_to_ids()`**: 분리된 토큰을 어휘 사전의 고유 ID(정수)로 변환합니다.
      ```python
      kobart.tokenizer.convert_tokens_to_ids(tokens)
      # 출력: [16814, 14397, 14125, 25429, 1700, 10021, 10277, 9747]
      ```
  3.  **`encode()`**: `tokenize`와 `convert_tokens_to_ids`를 한 번에 수행하고, 문장의 시작(`<s>`)과 끝(`</s>`)을 알리는 스페셜 토큰을 추가합니다.
      ```python
      token_ids = kobart.tokenizer.encode('혼자 만들면서 배우는 딥러닝')
      # 출력: [0, 16814, 14397, 14125, 25429, 1700, 10021, 10277, 9747, 1]
      # 0: <s> (문장 시작), 1: </s> (문장 끝)
      ```
  4.  **`decode()`**: 토큰 ID 리스트를 다시 사람이 읽을 수 있는 문장으로 복원합니다.
      ```python
      kobart.tokenizer.decode(token_ids)
      # 출력: '<s>혼자 만들면서 배우는 딥러닝</s>'
      ```

---

## 3. 스터디 발표 포인트

- **허깅페이스 `pipeline`의 편리함**: 단 몇 줄의 코드로 최신 NLP 모델을 가져와 특정 태스크(요약, 감성 분석 등)를 수행할 수 있다는 점을 강조.
- **BART 모델의 특징**:
    - **인코더-디코더 구조**: BERT(인코더)와 GPT(디코더)의 장점을 결합한 모델.
    - **Denoising Autoencoder**: 입력 문장에 노이즈(마스킹 등)를 주고 원본 문장을 복원하도록 학습하여, 문맥 이해 및 생성 능력이 뛰어남. 이것이 요약과 같은 생성 작업에 BART가 강력한 이유임을 설명.
- **언어별 모델의 필요성**: 왜 영어 요약 모델로 한국어를 처리할 수 없는지 설명. (학습 데이터와 토크나이저가 해당 언어에 특화되어 있기 때문)
- **토큰화의 중요성**: 자연어(텍스트)를 컴퓨터가 이해할 수 있는 숫자(벡터)로 바꾸는 첫 단계이며, 모델의 성능에 큰 영향을 미친다는 점을 설명. 특히 KoBART가 사용하는 SentencePiece 토크나이저가 한국어의 교착어 특성을 어떻게 처리하는지 간단히 언급하면 좋음.
- **모델 구조의 핵심**:
    - **인코더**: 입력 문장의 의미를 압축하는 역할.
    - **디코더**: 압축된 의미를 바탕으로 새로운 문장(요약문)을 생성하는 역할.
    - 이 두 가지가 어떻게 상호작용하여 요약이 이루어지는지 개념적으로 설명.
