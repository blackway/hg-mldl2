# [발표자료] 10-2: 트랜스포머로 상품 설명 요약하기

---

## 🎯 학습 목표

1.  Hugging Face의 `pipeline`을 사용하여 텍스트 요약 모델을 사용하는 방법을 이해한다.
2.  영어 모델과 한국어 모델의 차이를 알고, 특정 언어에 맞는 모델을 로드하여 활용할 수 있다.
3.  요약 모델의 내부 구조(Configuration, Tokenizer)가 어떻게 구성되어 있는지 파악한다.

---

## 📝 노트북 셀 단위 상세 설명

### **Cell 1-4: 시작하기 - 파이프라인 임포트**

#### **무엇을 하는 코드인가요?**

- **라이브러리 임포트**: Hugging Face의 `transformers` 라이브러리에서 `pipeline` 함수를 불러옵니다.
- **파이프라인**: 자연어 처리, 컴퓨터 비전 등 복잡한 작업을 몇 줄의 코드로 간단하게 수행할 수 있도록 모델 로딩, 전처리, 추론 과정을 하나로 묶어놓은 고수준 API입니다.

```python
from transformers import pipeline
```

---

### **Cell 5-7: 기본 요약 파이프라인 사용하기 (영어)**

#### **무엇을 하는 코드인가요?**

`pipeline`을 사용하여 기본적인 텍스트 요약 모델을 로드하고, 영어 텍스트를 요약하는 예제입니다.

1.  **파이프라인 생성 (`pipeline(task='summarization')`)**
    - `task='summarization'`: 텍스트 요약 작업을 수행할 것이라고 명시합니다.
    - 모델을 지정하지 않으면, Hugging Face는 해당 작업의 기본 모델(여기서는 `sshleifer/distilbart-cnn-12-6`)을 자동으로 다운로드하여 로드합니다. 이 모델은 영어 뉴스 기사 요약에 특화되어 있습니다.

2.  **영어 텍스트 요약**
    - 반 고흐에 대한 영어 설명을 모델에 입력하여 요약 결과를 확인합니다.
    - 모델이 핵심 내용을 잘 추출하여 간결한 문장으로 만들어내는 것을 볼 수 있습니다.

```python
# Cell 6: 요약 파이프라인 생성 (기본 영어 모델)
pipe = pipeline(task='summarization',
                model='sshleifer/distilbart-cnn-12-6', device=0)

# Cell 7: 영어 텍스트 요약 실행
sample_text = """Vincent Willem van Gogh was a Dutch Post-Impressionist painter ..."""
pipe(sample_text)
```

---

### **Cell 8-9: 한국어 요약 모델(KoBART) 로드 및 사용**

#### **무엇을 하는 코드인가요?**

기본 영어 모델로는 한국어를 처리할 수 없으므로, 한국어 요약에 특화된 **KoBART** 모델을 로드하여 상품 설명을 요약합니다.

1.  **KoBART 모델 로드**
    - `model='EbanLee/kobart-summary-v3'`: Hugging Face Hub에 공개된 한국어 요약 모델의 이름을 명시적으로 지정합니다.
    - `pipeline`은 이 모델과 관련된 설정(configuration) 및 토크나이저(tokenizer)를 함께 다운로드합니다.

2.  **한국어 텍스트 요약**
    - '혼자 공부하는 데이터 분석' 책의 긴 상품 설명을 입력합니다.
    - KoBART 모델이 이 설명을 분석하여 핵심 내용(7단계 구성, 스토리 기반 학습, 동영상 강의 지원, 용어 노트 제공 등)을 간결하게 요약해줍니다.

```python
# Cell 8: 한국어 요약 모델(KoBART) 로드
kobart = pipeline(task='summarization',
                  model='EbanLee/kobart-summary-v3', device=0)

# Cell 9: 한국어 텍스트 요약 실행
ko_text = """하나, ‘입문자 맞춤형 7단계 구성’을 따라가며 체계적으로 반복하는 탄탄한 학습 설계!..."""
kobart(ko_text)
```

---

### **Cell 10-11: 모델 내부 들여다보기**

#### **무엇을 하는 코드인가요?**

로드된 KoBART 모델의 설정(Configuration)과 구조(Architecture)를 확인하여 모델의 특징을 파악합니다.

- **`kobart.model.config`**: 모델의 하이퍼파라미터와 설정을 담고 있는 객체입니다.
  - 요약 작업에 사용되는 파라미터(`max_length`, `num_beams` 등)와 모델의 기본 정보(인코더/디코더 레이어 수, 어휘 사전 크기 등)를 확인할 수 있습니다.
- **`kobart.model`**: 실제 모델의 아키텍처를 보여줍니다.
  - 인코더-디코더 구조를 가진 BART 모델의 구성 요소(임베딩 층, 어텐션 층, 레이어 정규화 등)를 상세히 볼 수 있습니다.

---

### **Cell 12-19: 텍스트 토큰화 과정 이해하기**

#### **무엇을 하는 코드인가요?**

모델이 텍스트를 이해하는 첫 단계인 **토큰화(Tokenization)** 과정을 단계별로 실습합니다.

1.  **어휘 사전 (`vocab`)**: 모델이 알고 있는 단어(토큰)의 집합입니다. KoBART는 약 30,000개의 토큰으로 구성된 사전을 가집니다.
2.  **`tokenize()`**: 문장을 토큰 단위로 분해합니다. 한국어의 경우, 의미 단위(예: '만들면서' -> ' 만들', '면서')로 쪼개는 것을 볼 수 있습니다.
3.  **`convert_tokens_to_ids()`**: 분해된 토큰을 어휘 사전에 있는 고유한 정수(ID)로 변환합니다.
4.  **`encode()`**: `tokenize()`와 `convert_tokens_to_ids()` 과정을 한 번에 처리하고, 문장의 시작과 끝을 알리는 특수 토큰(`<s>`, `</s>`)을 추가합니다.
5.  **`decode()`**: 정수 ID 시퀀스를 다시 사람이 읽을 수 있는 문장으로 복원합니다.

```python
# Cell 15: 문장을 토큰으로 분해
tokens = kobart.tokenizer.tokenize('혼자 만들면서 배우는 딥러닝')
# 결과: [' 혼자', ' 만들', '면서', ' 배우는', ' ', '딥', '러', '닝']

# Cell 17: 문장을 한번에 정수 시퀀스로 변환
token_ids = kobart.tokenizer.encode('혼자 만들면서 배우는 딥러닝')
# 결과: [0, 16814, 14397, 14125, 25429, 1700, 10021, 10277, 9747, 1]
```

---

## 🏁 결론 및 요약

- **Hugging Face `pipeline`**을 사용하면 복잡한 트랜스포머 모델도 손쉽게 활용할 수 있습니다.
- 텍스트 요약과 같은 NLP 작업은 **언어에 맞는 특화된 모델**을 사용하는 것이 중요합니다. (영어 -> `distilbart`, 한국어 -> `KoBART`)
- 모델은 텍스트를 **토큰화**하여 숫자 시퀀스로 변환한 뒤 처리하며, 이 과정을 이해하는 것은 모델의 동작 원리를 파악하는 데 도움이 됩니다.
