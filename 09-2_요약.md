# 09-2.ipynb: 순환 신경망(RNN)을 이용한 IMDB 리뷰 분류 요약

이 Jupyter Notebook은 순환 신경망(RNN)을 사용하여 IMDB 영화 리뷰의 감성을 분석(긍정/부정 분류)하는 과정을 다룹니다.

## 1. 데이터 준비 및 전처리

- **IMDB 데이터셋 로드**: `keras.datasets.imdb`를 통해 영화 리뷰 데이터를 불러옵니다. 데이터는 이미 정수 시퀀스로 변환되어 있으며, 각 정수는 특정 단어를 나타냅니다.
- **데이터 분할**: 훈련, 검증, 테스트 세트로 데이터를 나눕니다.
- **패딩 (Padding)**: 리뷰마다 길이가 다르므로, `pad_sequences`를 사용하여 모든 리뷰의 길이를 동일하게 맞춥니다 (예: 100개 단어). 길이가 짧은 리뷰는 0으로 채워집니다.

## 2. 순환 신경망 (RNN) 모델 구축

노트북에서는 두 가지 접근 방식으로 RNN 모델을 만듭니다.

### 접근 방식 1: 원-핫 인코딩

- **방식**: 각 단어(정수)를 어휘 사전 크기의 벡터로 변환합니다. 해당 단어의 인덱스만 1이고 나머지는 모두 0입니다.
- **구조**: `SimpleRNN` 레이어와 `Dense` 출력 레이어로 구성됩니다.
- **단점**: 단어 수가 많아지면 입력 데이터의 크기가 매우 커져 메모리 비효율성을 야기합니다.

### 접근 방식 2: 단어 임베딩 (Word Embedding)

- **방식**: 원-핫 인코딩의 비효율성을 해결하기 위해 `Embedding` 레이어를 사용합니다. 각 단어를 저차원의 밀집 벡터(dense vector)로 변환하여 단어의 의미적 관계를 학습합니다.
- **구조**: `Embedding` 레이어 -> `SimpleRNN` 레이어 -> `Dense` 출력 레이어.
- **장점**: 메모리 효율성이 높고, 단어 간의 의미적 유사성을 포착할 수 있어 일반적으로 성능이 더 좋습니다.

## 3. 모델 훈련 및 평가

- **모델 컴파일**:
    - **Optimizer**: `adam`
    - **Loss Function**: `binary_crossentropy` (이진 분류 문제이므로)
    - **Metrics**: `accuracy` (정확도)
- **콜백 (Callbacks) 활용**:
    - `ModelCheckpoint`: 훈련 중 검증 세트에서 가장 좋은 성능을 보인 모델의 가중치를 저장합니다.
    - `EarlyStopping`: 검증 손실이 일정 기간 개선되지 않으면 훈련을 조기 종료하여 과적합을 방지합니다.
- **훈련 및 평가**:
    - `fit()` 메소드로 모델을 훈련합니다.
    - 훈련 과정의 손실(loss)과 정확도(accuracy)를 그래프로 시각화하여 과적합 여부를 확인합니다.

## 결론

이 노트북은 텍스트 분류 문제를 해결하기 위해 RNN을 사용하는 기본적인 파이프라인을 보여줍니다. 특히, 자연어 처리에서 **단어 임베딩**의 중요성과 효율성을 강조하는 훌륭한 실습 자료입니다.
