# 혼자 공부하는 머신러닝 김러닝 개정판 요약.

### 9장 1절: 순차 데이터와 순환 신경망

이 영상은 '혼자 공부하는 머신 러닝 딥러닝' 책의 9장 1절 내용을 설명하며, 순차 데이터와 순환 신경망(RNN, Recurrent Neural Network)의 개념을 소개합니다.

* **순차 데이터**: 순서가 중요한 데이터로, 텍스트나 시간에 따라 발생하는 시계열 데이터(예: 온도, 센서 데이터)가 이에 해당합니다. 기존의 일반 인공 신경망이나 합성곱 신경망은 이런 순서 정보를 활용하기 어렵기 때문에, 순차 데이터에 맞는 새로운 모델이 필요합니다.

* **순환 신경망(RNN)**: 순차 데이터를 처리하기 위해 고안된 신경망으로, **순환 고리**를 가지고 있습니다.
    * **순환 고리**: 뉴런의 출력이 다음 층뿐만 아니라 자기 자신에게도 되돌아오는 구조를 말합니다. 
    * **데이터 재사용**: 이 순환 고리를 통해 이전 단계(타임스텝)의 처리 결과를 다음 단계의 데이터 처리 시 재사용합니다. 예를 들어 'A'를 처리한 결과가 'B'를 처리할 때 사용되고, 'B'를 처리한 결과가 'C'를 처리할 때 사용되는 방식입니다.
    * **은닉 상태(Hidden State)**: 순환 신경망의 출력값은 보통 '은닉 상태'라고 부르며, 흔히 'H'로 표현합니다.
    * **가중치 공유**: 순환 신경망은 여러 타임스텝을 처리할 때 같은 가중치를 사용합니다. 각 타임스텝마다 다른 가중치를 쓰는 것이 아닙니다.

---

### 순환 신경망의 특징

* **입력 데이터의 형태**: 순차 데이터는 하나의 샘플이 여러 개의 타임스텝으로 구성되어 있습니다. 예를 들어, "I am a boy"라는 문장은 'I', 'am', 'a', 'boy'라는 여러 타임스텝으로 이루어지며, 각 단어는 고정된 크기의 벡터로 표현됩니다.
* **모델 구성**: 순환 신경망은 순차 데이터의 차원을 하나로 합쳐서 다음 층으로 전달합니다. 그래서 합성곱 신경망과는 다르게 데이터를 펼치는(flatten) 과정 없이 바로 밀집층(완전 연결층)에 연결해서 분류 등의 문제를 해결할 수 있습니다.
* **층을 쌓을 경우**: 여러 개의 순환층을 쌓을 때는 첫 번째 순환층은 모든 타임스텝의 출력을 다음 층으로 전달해야 합니다. 반면, 마지막 순환층은 최종 타임스텝의 출력만 사용하도록 구성하는 것이 일반적입니다.

---

아래 그림으로 순환 신경망(RNN)의 핵심 개념을 쉽게 설명해 드릴게요.

### 순환 신경망의 작동 방식

아래 그림은 순환 신경망이 데이터를 처리하는 과정을 시간 순서대로 펼쳐놓은 것입니다.

http://googleusercontent.com/image_generation_content/1

* **1단계: 입력 데이터 (Input)**
    * `x(t-1)`, `x(t)`, `x(t+1)`은 순차적으로 들어오는 데이터(예: "나는", "소년", "이다" 와 같은 단어)를 의미합니다. 순환 신경망은 이 데이터를 **순서대로 하나씩** 처리합니다.

* **2단계: 정보 전달 (Hidden State)**
    * `h(t-1)`은 첫 번째 데이터 `x(t-1)`을 처리하고 난 후의 결과입니다. 이 결과는 `h(t)`라는 이름으로 다음 단계인 `x(t)`를 처리할 때 재사용됩니다.
    * 마찬가지로, `x(t)`를 처리한 결과인 `h(t)`는 그 다음 단계인 `x(t+1)`을 처리할 때 다시 사용됩니다.
    * 이처럼 이전 단계의 정보를 **기억**하고 다음 단계로 **전달**하기 때문에, 순환 신경망은 데이터의 순서와 맥락을 이해할 수 있습니다. 이것이 바로 순환 신경망의 가장 중요한 특징입니다.

* **3단계: 가중치 공유**
    * 그림에서는 여러 개의 박스가 보이지만, 사실 이 박스들은 모두 **하나의 신경망**입니다. 이 신경망은 모든 단계에서 똑같은 가중치(`W_rec`, `W_in`)를 사용해서 데이터를 처리합니다. 이렇게 가중치를 공유하기 때문에, 순차 데이터의 길이와 상관없이 하나의 모델로 학습할 수 있습니다.
