# 10-3장: 대규모 언어 모델로 텍스트 생성하기
## 그룹 스터디 발표 자료

---

## 📋 목차
1. [학습 목표](#학습-목표)
2. [대규모 언어 모델 개요](#대규모-언어-모델-개요)
3. [EXAONE-3.5 모델 실습](#exaone-35-모델-실습)
4. [토큰 디코딩 전략](#토큰-디코딩-전략)
5. [GPT-4o 비교 실습](#gpt-4o-비교-실습)
6. [핵심 개념 정리](#핵심-개념-정리)
7. [Q&A](#qa)

---

## 🎯 학습 목표

### 이번 장에서 배울 내용
- **대규모 언어 모델(LLM)**의 기본 개념과 활용법
- **EXAONE-3.5** 모델을 활용한 한국어 텍스트 생성
- **토큰 디코딩 전략**의 종류와 특징
  - Temperature 조절
  - Top-k 샘플링
  - Top-p (nucleus) 샘플링
- **OpenAI GPT-4o**와의 비교 분석
- 실무에서 활용 가능한 챗봇 구현 사례

---

## 🤖 대규모 언어 모델 개요

### LLM이란?
- **Large Language Model**의 약자
- 수십억 개 이상의 매개변수를 가진 신경망 모델
- 대량의 텍스트 데이터로 사전 훈련된 생성형 AI

### 주요 특징
- **자연어 이해**: 인간의 언어를 이해하고 맥락 파악
- **텍스트 생성**: 주어진 프롬프트에 따라 자연스러운 텍스트 생성
- **다양한 태스크**: 번역, 요약, 질답, 창작 등 다목적 활용
- **Few-shot Learning**: 적은 예시로도 새로운 태스크 수행

### 국내외 주요 LLM
- **OpenAI**: GPT-4, GPT-4o
- **LG AI Research**: EXAONE-3.5
- **Google**: Gemini, PaLM
- **Meta**: LLaMA
- **Anthropic**: Claude

---

## 🇰🇷 EXAONE-3.5 모델 실습

### EXAONE-3.5 소개
- **LG AI Research**에서 개발한 한국어 특화 LLM
- **2.4B(24억) 매개변수** 모델 사용
- 한국어와 영어 모두 지원
- **상업적 이용 가능**한 오픈소스 모델

### 모델 로딩 및 설정

```python
from transformers import AutoTokenizer, pipeline

# 토크나이저 로딩
exaone_tokenizer = AutoTokenizer.from_pretrained(
    "LGAI-EXAONE/EXAONE-3.5-2.4B-Instruct"
)

# 파이프라인 설정
pipe = pipeline(
    task="text-generation",
    model="LGAI-EXAONE/EXAONE-3.5-2.4B-Instruct",
    tokenizer=exaone_tokenizer,
    device=0,  # GPU 사용 (CPU는 -1)
    trust_remote_code=True
)
```

### 실무 활용 사례: 쇼핑몰 Q&A 챗봇

#### 시스템 프롬프트 설계
```python
messages = [
    {
        "role": "system",
        "content": """너는 쇼핑몰 홈페이지에 올라온 질문에 대답하는 Q&A 챗봇이야.
                     확정적인 답변을 하지 말고 제품 담당자가 정확한 답변을 하기 위해
                     시간이 필요하다는 간단하고 친절한 답변을 생성해줘."""
    },
    {
        "role": "user", 
        "content": "이 다이어리에 내년도 공휴일이 표시되어 있나요?"
    }
]
```

#### 기본 생성 결과
```
안녕하세요! 다이어리에 내년의 공휴일이 미리 표시되어 있는지에 대해 
정확한 답변을 드리기 위해서는 제품 담당자에게 확인이 필요합니다. 
현재로선 직접 확인이 어려우니, 저희가 안내드릴 수 있는 방법으로는 
고객센터에 연락하시거나, 제품 페이지 내의 문의 게시판을 통해 
질문해 보시는 것이 좋을 것 같습니다. 
담당자분께서 빠르게 답변해 주실 거예요! 감사합니다.
```

### 핵심 매개변수
- **max_new_tokens**: 생성할 최대 토큰 수 (200)
- **return_full_text**: 전체 대화 반환 여부 (False로 설정시 생성된 부분만)
- **do_sample**: 샘플링 사용 여부 (True/False)

---

## 🎲 토큰 디코딩 전략

### 1. 기본 샘플링과 Temperature

#### Temperature란?
- **모델의 창의성과 일관성을 조절하는 매개변수**
- 값이 낮을수록 → 보수적이고 일관된 출력
- 값이 높을수록 → 창의적이고 다양한 출력

#### 수학적 원리
```python
import numpy as np
from scipy.special import softmax

# 로짓 예시
logits = np.array([1, 2, 3, 4, 100])

# Temperature = 1.0 (기본)
probas_normal = softmax(logits)
print("기본:", probas_normal)
# 결과: [거의 0, 거의 0, 거의 0, 거의 0, 거의 1.0]

# Temperature = 100 (높은 값)
probas_high = softmax(logits/100)
print("높은 temperature:", probas_high)
# 결과: [0.22, 0.13, 0.10, 0.12, 0.43] - 더 균등한 분포
```

#### Temperature 실험 결과

**Temperature = 10.0 (매우 높음)**
```
저는 해당Qy 관리자 입장이지않그래서요qing 특정 브랜드 또는 제조업의 
공휴일 포함사항들 자세힌 조회 권한 없으므로... 
(→ 비일관적이고 이상한 출력)
```

**Temperature = 0.001 (매우 낮음)**
```
안녕하세요! 다이어리에 내년의 공휴일이 미리 표시되어 있는지에 대해 
정확한 답변을 드리기 위해서는 제품 담당자에게 확인이 필요합니다...
(→ 일관되고 안정적인 출력)
```

### 2. Top-k 샘플링

#### 개념
- **상위 k개의 토큰**에서만 샘플링
- 확률이 낮은 토큰들을 제거하여 품질 향상
- k값이 작을수록 더 보수적

#### 실험 결과 (k=10)
```python
output = pipe(messages, max_new_tokens=200, return_full_text=False,
              do_sample=True, top_k=10)
```

```
안녕하세요! 다이어리 정보에 대해 궁금하시군요. 
죄송하지만, 실시간으로 모든 디테일을 확인하기는 어렵습니다. 
정확한 답변을 드리려면 제품 담당자님께 직접 문의하시는 게 
가장 좋을 것 같아요...
```

### 3. Top-p (Nucleus) 샘플링

#### 개념
- **누적 확률이 p에 도달할 때까지의 토큰들**에서 샘플링
- 동적으로 후보 토큰 수가 결정됨
- p값이 낮을수록 더 집중적인 선택

#### 장점
- **적응적**: 상황에 따라 후보 수가 변함
- **균형적**: 너무 보수적이지도 창의적이지도 않음

#### 실험 결과 (p=0.9)
```python
output = pipe(messages, max_new_tokens=200, return_full_text=False,
              do_sample=True, top_p=0.9)
```

### 4. 복합 전략

#### Temperature + Top-k + Top-p 조합
```python
output = pipe(messages, max_new_tokens=200, return_full_text=False,
              do_sample=True, temperature=0.8, top_k=100, top_p=0.9)
```

**결과**: 창의성과 일관성의 균형을 맞춘 고품질 출력

---

## 🚀 GPT-4o 비교 실습

### OpenAI API 사용법

```python
from openai import OpenAI

client = OpenAI(api_key="your-api-key-here")

completion = client.chat.completions.create(
    model="gpt-4o-mini",
    messages=messages
)

print(completion.choices[0].message.content)
```

### 모델 비교 분석

| 특성 | EXAONE-3.5 | GPT-4o |
|------|------------|--------|
| **언어 지원** | 한국어 특화 | 다국어 지원 |
| **모델 크기** | 2.4B 매개변수 | 수백억 매개변수 (추정) |
| **응답 품질** | 한국어에서 자연스러움 | 전반적으로 높은 품질 |
| **비용** | 로컬 실행 가능 (무료) | API 사용료 |
| **속도** | GPU 환경에서 빠름 | 네트워크 지연 |
| **커스터마이징** | 높음 (오픈소스) | 제한적 |

### 같은 질문에 대한 응답 비교

**EXAONE-3.5 응답:**
```
안녕하세요! 다이어리에 내년의 공휴일이 미리 표시되어 있는지에 대해 
정확한 답변을 드리기 위해서는 제품 담당자에게 확인이 필요합니다...
```

**GPT-4o 응답:**
```
안녕하세요! 제품 담당자가 정확한 정보를 확인한 후에 
답변드릴 수 있도록 시간이 필요합니다. 
조금만 기다려 주시면 감사하겠습니다!
```

---

## 📚 핵심 개념 정리

### 1. 대규모 언어 모델의 이해
- **사전 훈련**: 대량의 텍스트 데이터로 언어 패턴 학습
- **미세 조정**: 특정 태스크에 맞게 추가 훈련
- **프롬프트 엔지니어링**: 효과적인 입력 설계의 중요성

### 2. 토큰 디코딩 전략의 실무 적용

#### 상황별 권장 설정

| 사용 목적 | Temperature | Top-k | Top-p | 특징 |
|-----------|-------------|-------|-------|------|
| **일관된 고객 서비스** | 0.1-0.3 | 10-50 | 0.8-0.9 | 안정적, 예측 가능 |
| **창의적 글쓰기** | 0.7-1.0 | 100+ | 0.9-0.95 | 다양하고 창의적 |
| **기술 문서 작성** | 0.1-0.2 | 10-20 | 0.7-0.8 | 정확하고 간결 |
| **일반 대화** | 0.5-0.7 | 50-100 | 0.9 | 자연스럽고 균형적 |

### 3. 모델 선택 기준
- **한국어 품질**: EXAONE-3.5가 우수
- **범용성**: GPT-4o가 우수
- **비용 효율성**: 로컬 모델(EXAONE) 유리
- **최신 정보**: API 기반 모델 유리

### 4. 프롬프트 엔지니어링 팁

#### 효과적인 시스템 프롬프트 작성법
1. **역할 정의**: "너는 ~이야"
2. **행동 지침**: "~하지 말고", "~해줘"
3. **톤앤매너**: "친절하게", "전문적으로"
4. **제약 조건**: "200자 이내로", "bullet point로"

#### 사용자 프롬프트 개선법
1. **구체적 명시**: 모호한 표현 피하기
2. **예시 제공**: Few-shot learning 활용
3. **단계별 요청**: 복잡한 태스크 분할
4. **출력 형식 지정**: JSON, 표 등 구조화

---

## 🔧 실무 활용 시나리오

### 1. 고객 서비스 챗봇
```python
system_prompt = """
당신은 전자제품 쇼핑몰의 고객 서비스 담당자입니다.
- 친근하고 전문적인 톤으로 응답하세요
- 확실하지 않은 정보는 담당자 확인 후 답변한다고 안내하세요
- 고객의 불만에는 공감하며 해결책을 제시하세요
"""
```

### 2. 상품 설명 생성기
```python
system_prompt = """
당신은 마케팅 카피라이터입니다.
- 상품의 핵심 기능과 장점을 강조하세요
- 감정에 호소하는 표현을 사용하세요
- 구매 욕구를 자극하는 Call-to-Action을 포함하세요
- 50-100자 내외로 간결하게 작성하세요
"""
```

### 3. 이메일 자동 답장
```python
system_prompt = """
당신은 비즈니스 이메일 작성 도우미입니다.
- 정중하고 전문적인 비즈니스 톤을 사용하세요
- 요점을 명확하게 전달하세요
- 필요시 다음 단계를 제안하세요
- 적절한 인사말과 마무리 인사를 포함하세요
"""
```

---

## ⚠️ 주의사항 및 한계점

### 1. 기술적 한계
- **환각(Hallucination)**: 잘못된 정보 생성 가능
- **편향(Bias)**: 훈련 데이터의 편향성 반영
- **맥락 길이**: 토큰 수 제한으로 긴 대화 어려움
- **실시간 정보**: 훈련 시점 이후 정보 부족

### 2. 윤리적 고려사항
- **개인정보 보호**: 민감한 정보 입력 금지
- **저작권**: 생성된 콘텐츠의 저작권 문제
- **책임성**: AI 생성 콘텐츠의 책임 소재
- **투명성**: AI 사용 사실 공개 필요성

### 3. 실무 도입 시 체크리스트
- [ ] 데이터 보안 정책 수립
- [ ] 모델 성능 모니터링 체계
- [ ] 사용자 피드백 수집 방안
- [ ] 오류 대응 프로세스
- [ ] 정기적 모델 업데이트 계획

---

## 🚀 향후 발전 방향

### 1. 기술적 발전
- **멀티모달**: 텍스트 + 이미지 + 음성 통합
- **더 긴 맥락**: 토큰 수 제한 완화
- **효율성 개선**: 더 작은 모델로 동등한 성능
- **실시간 학습**: 동적 지식 업데이트

### 2. 산업별 특화
- **의료**: 의료진 지원, 환자 상담
- **교육**: 개인화 학습, 자동 채점
- **법률**: 계약서 검토, 법률 자문
- **금융**: 투자 분석, 리스크 평가

### 3. 접근성 향상
- **API 표준화**: 모델 간 호환성
- **비용 절감**: 오픈소스 모델 확산
- **사용성 개선**: No-code/Low-code 도구
- **다국어 지원**: 소수 언어 지원 확대

---

## 💡 실습 과제

### 과제 1: 다양한 프롬프트 실험
1. 같은 질문에 대해 다른 시스템 프롬프트 적용
2. Temperature 값을 0.1, 0.5, 1.0으로 변경하며 결과 비교
3. Top-k와 Top-p 조합별 성능 분석

### 과제 2: 실무 시나리오 구현
1. 자신의 전공/관심 분야에 맞는 챗봇 프롬프트 설계
2. EXAONE과 GPT 모델로 같은 태스크 수행 후 비교
3. 최적의 파라미터 조합 찾기

### 과제 3: 성능 평가
1. 생성된 텍스트의 품질 평가 기준 수립
2. 다양한 디코딩 전략의 장단점 분석
3. 비용 대비 효과 분석

---

## 🔍 Q&A

### 자주 묻는 질문들

**Q1: EXAONE과 GPT 중 어떤 것을 선택해야 하나요?**
- 한국어 위주 + 비용 절감 → EXAONE
- 다국어 + 최고 품질 → GPT
- 프로토타입 → EXAONE, 상용화 → 요구사항에 따라

**Q2: Temperature가 높으면 항상 나쁜가요?**
- 창의적 작업(소설, 마케팅 카피)에서는 높은 값 유리
- 정확성이 중요한 작업에서는 낮은 값 권장
- 용도에 따라 적절한 값 선택 필요

**Q3: API 키 없이 GPT를 사용할 수 있나요?**
- 공식적으로는 불가능
- Hugging Face의 오픈소스 대안 모델 활용 가능
- EXAONE, LLaMA 등 무료 모델 고려

**Q4: 프롬프트 엔지니어링의 핵심은 무엇인가요?**
- 명확하고 구체적인 지시
- 적절한 예시 제공
- 단계별 사고 유도
- 지속적인 실험과 개선

---

## 📖 추가 학습 자료

### 권장 도서
- "The Prompt Engineer's Handbook"
- "Large Language Models: A Complete Guide"
- "Generative AI for Practitioners"

### 온라인 리소스
- Hugging Face Transformers 문서
- OpenAI API 가이드
- Prompt Engineering Guide (GitHub)
- Papers with Code - NLP 섹션

### 실습 환경
- Google Colab (무료 GPU)
- Hugging Face Spaces
- 로컬 Jupyter Lab 환경

---

## 🎉 마무리

### 오늘 배운 핵심 내용
1. **대규모 언어 모델**의 기본 원리와 활용법
2. **EXAONE-3.5**를 활용한 한국어 텍스트 생성
3. **토큰 디코딩 전략**의 이해와 실무 적용
4. **프롬프트 엔지니어링**의 중요성과 기법

### 다음 단계
- 개인 프로젝트에 LLM 적용해보기
- 다양한 오픈소스 모델 실험
- 프롬프트 엔지니어링 기술 향상
- 실무 도입을 위한 기술적 준비

### 감사합니다!
질문이 있으시면 언제든지 말씀해주세요! 🙋‍♂️🙋‍♀️