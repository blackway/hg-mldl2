# [발표자료] 09-3: LSTM과 GRU 셀을 이용한 영화 리뷰 감성 분석

---

## 🎯 학습 목표

1.  RNN의 장기 의존성 문제를 이해하고, 이를 해결하기 위한 LSTM과 GRU의 필요성을 안다.
2.  Keras를 사용하여 LSTM과 GRU 신경망을 직접 구축하고 훈련할 수 있다.
3.  드롭아웃, 층 쌓기 등 순환 신경망의 성능을 높이는 기법들을 적용하고 효과를 분석한다.

---

## 📝 노트북 셀 단위 상세 설명

### **Cell 1-3: 시작하기**

#### **무엇을 하는 코드인가요?**

- **라이브러리 임포트**: 모델 구축에 필요한 `keras`와 `tensorflow`를 불러옵니다.
- **랜덤 시드 고정**: 모델을 훈련할 때마다 동일한 결과를 얻기 위해 랜덤 시드를 42로 고정합니다. 이는 가중치 초기화나 데이터 셔플링 등 무작위성을 제어하여 실험의 재현성을 보장합니다.

```python
# 실행마다 동일한 결과를 얻기 위해 케라스에 랜덤 시드를 사용하고 텐서플로 연산을 결정적으로 만듭니다.
import keras
import tensorflow as tf

keras.utils.set_random_seed(42)
tf.random.set_seed(42)
tf.config.experimental.enable_op_determinism()
```

---

### **Cell 4-6: 데이터 로드 및 전처리**

#### **무엇을 하는 코드인가요?**

IMDB 영화 리뷰 데이터셋을 불러와 모델이 학습할 수 있는 형태로 가공합니다.

1.  **데이터 로드 (`imdb.load_data`)**
    - `num_words=500`: 데이터셋에서 가장 자주 사용되는 단어 500개만 사용하도록 제한합니다. 어휘사전의 크기를 500으로 줄여 모델을 더 가볍고 빠르게 만듭니다.
    - 로드된 데이터는 이미 각 단어가 고유한 정수로 변환되어 있습니다.

2.  **훈련/검증 세트 분리 (`train_test_split`)**
    - 훈련 데이터를 다시 훈련 세트(80%)와 검증 세트(20%)로 나눕니다.
    - **훈련 세트**: 모델을 직접 학습시키는 데 사용됩니다.
    - **검증 세트**: 모델 훈련 중 성능을 검증하며, 과대적합 발생 여부를 판단하는 기준이 됩니다.

3.  **패딩 (`pad_sequences`)**
    - **필요성**: 각 영화 리뷰의 단어 개수(시퀀스 길이)가 모두 다르기 때문에, 모델에 입력으로 넣기 위해 길이를 통일시켜야 합니다.
    - `maxlen=100`: 모든 리뷰의 길이를 100으로 맞춥니다.
        - 100보다 긴 리뷰는 앞부분을 잘라냅니다.
        - 100보다 짧은 리뷰는 앞부분을 0으로 채웁니다 (이를 **패딩(padding)**이라고 합니다).

```python
# Cell 5: 데이터 로드 및 분리
from keras.datasets import imdb
from sklearn.model_selection import train_test_split

(train_input, train_target), (test_input, test_target) = imdb.load_data(num_words=500)
train_input, val_input, train_target, val_target = train_test_split(
    train_input, train_target, test_size=0.2, random_state=42)

# Cell 6: 시퀀스 패딩
from keras.preprocessing.sequence import pad_sequences

train_seq = pad_sequences(train_input, maxlen=100)
val_seq = pad_sequences(val_input, maxlen=100)
```

---

### **Cell 7-9: 첫 번째 모델 - 기본 LSTM**

#### **무엇을 하는 코드인가요?**

가장 기본적인 LSTM 모델을 만들고 훈련시킨 후, 그 결과를 분석합니다.

1.  **모델 구축 (`keras.Sequential`)**
    - `Embedding(500, 16)`: 단어 임베딩 층입니다. 500개의 단어를 각각 16차원의 의미 벡터로 변환합니다.
    - `LSTM(8)`: LSTM 층입니다. 8개의 유닛을 가지며, 시퀀스 데이터를 처리하여 주요 특징을 추출합니다.
    - `Dense(1, activation='sigmoid')`: 최종 출력 층입니다. 긍정(1) 또는 부정(0)을 예측하는 이진 분류이므로 1개의 뉴런과 시그모이드 함수를 사용합니다.

2.  **모델 컴파일 및 훈련 (`.compile`, `.fit`)**
    - `optimizer='adam'`, `loss='binary_crossentropy'`: 이진 분류 문제에 표준적으로 사용되는 옵티마이저와 손실 함수를 설정합니다.
    - **Callbacks**: 훈련 과정을 제어합니다.
        - `ModelCheckpoint`: 매 에포크마다 검증 세트의 손실(`val_loss`)을 측정하여, 가장 낮은 손실을 기록한 모델을 `best-lstm-model.keras` 파일로 저장합니다.
        - `EarlyStopping`: 검증 손실이 3번의 에포크 동안 개선되지 않으면, 과대적합으로 판단하고 훈련을 조기 종료합니다. `restore_best_weights=True`는 훈련 종료 후 모델의 가중치를 가장 성능이 좋았던 시점으로 복원해줍니다.

3.  **결과 시각화 (`matplotlib.pyplot`)**
    - 훈련 과정의 손실(loss)과 검증 손실(val_loss)을 그래프로 그립니다.
    - **분석**: 훈련 손실은 계속 감소하는데 검증 손실이 다시 증가하는 지점이 보입니다. 이는 모델이 훈련 데이터에 과대적합되고 있음을 의미합니다.

---

### **Cell 10-13: 두 번째 모델 - 드롭아웃 적용 LSTM**

#### **무엇을 하는 코드인가요?**

앞서 발생한 과대적합 문제를 해결하기 위해 LSTM 층에 드롭아웃을 적용합니다.

1.  **드롭아웃 추가 (`dropout=0.2`)**
    - `LSTM(8, dropout=0.2)`: LSTM 층의 입력에 20% 비율로 드롭아웃을 적용합니다. 훈련 시마다 랜덤하게 일부 뉴런의 연결을 끊어, 모델이 특정 패턴에 과도하게 의존하는 것을 막고 일반화 성능을 높여줍니다.

2.  **훈련 및 결과 분석**
    - 동일한 방식으로 훈련하고 손실 그래프를 다시 그립니다.
    - **분석**: 드롭아웃 적용 후, 훈련 손실과 검증 손실의 차이가 크게 줄어들었습니다. 과대적합이 효과적으로 억제된 것을 확인할 수 있습니다.

---

### **Cell 14-17: 세 번째 모델 - 2개 층을 쌓은 LSTM**

#### **무엇을 하는 코드인가요?**

모델의 복잡도를 높여 더 정교한 특징을 학습하기 위해 LSTM 층을 2개로 쌓습니다.

1.  **층 쌓기 (`return_sequences=True`)**
    - `LSTM(..., return_sequences=True)`: 첫 번째 LSTM 층의 파라미터입니다. 이 값을 `True`로 설정해야 해당 층이 마지막 타임스텝의 출력만 내보내는 것이 아니라, 모든 타임스텝의 출력을 시퀀스 형태로 다음 LSTM 층으로 전달할 수 있습니다.

2.  **훈련 및 결과 분석**
    - 층이 깊어져 파라미터 수가 늘어났지만, 드롭아웃 덕분에 과대적합은 잘 제어되는 편입니다. 검증 손실도 이전 모델과 비슷하거나 약간 개선된 결과를 보입니다.

---

### **Cell 18-21: 네 번째 모델 - GRU 신경망**

#### **무엇을 하는 코드인가요?**

LSTM의 대안으로, 더 간단한 구조를 가진 GRU 셀을 사용해 모델을 만듭니다.

1.  **GRU 모델 구축**
    - `GRU(8, dropout=0.2)`: LSTM 층 대신 GRU 층을 사용합니다. GRU는 LSTM보다 게이트 수가 적어 구조가 더 간단하고, 계산해야 할 파라미터 수도 적습니다.
    - `model.summary()`를 통해 LSTM 모델과 파라미터 수를 비교해보면 GRU가 더 가볍다는 것을 확인할 수 있습니다.

2.  **훈련 및 결과 분석**
    - **분석**: GRU 모델은 LSTM 모델보다 더 적은 파라미터로도 비슷한, 혹은 약간 더 나은 검증 손실을 보여주었습니다. 이는 GRU가 계산 효율성과 성능 면에서 훌륭한 대안이 될 수 있음을 시사합니다.

---

### **Cell 22-23: 최종 평가**

#### **무엇을 하는 코드인가요?**

훈련 과정에서 가장 성능이 좋았던 모델(GRU 모델)을 불러와, 한 번도 본 적 없는 테스트 데이터로 최종 성능을 평가합니다.

1.  **최적 모델 로드 (`keras.models.load_model`)**
    - `ModelCheckpoint` 콜백이 저장해 둔 `best-gru-model.keras` 파일을 불러옵니다.

2.  **성능 평가 (`.evaluate`)**
    - 테스트 세트(test_seq, test_target)를 사용하여 모델의 최종 손실과 정확도를 계산합니다.
    - **결과**: 약 80.7%의 정확도를 달성했습니다. 이는 우리가 만든 모델이 처음 보는 영화 리뷰에 대해서도 10개 중 8개 정도는 긍정/부정을 올바르게 예측한다는 의미입니다.

---

## 🏁 결론 및 요약

- **LSTM과 GRU**는 RNN의 장기 의존성 문제를 해결하는 강력한 도구입니다.
- **드롭아웃**은 순환 신경망의 과대적합을 제어하는 데 필수적인 기법입니다.
- **GRU**는 LSTM보다 구조가 간단하고 계산량이 적지만, 많은 경우 비슷한 성능을 보여 효율적인 선택이 될 수 있습니다.
- 최종적으로 **GRU 모델**을 사용하여 IMDB 감성 분류 문제에서 **약 80.7%의 테스트 정확도**를 달성했습니다.
